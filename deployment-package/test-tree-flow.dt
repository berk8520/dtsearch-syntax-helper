// Test query for the new tree-based flow diagram
(dog OR cat) AND W/5 bird NOT "dead animal"

// Query with ambiguous operator precedence (should trigger errors):
(dog OR cat AND man) AND W/5 bird NOT "dead animal"

// More test cases for error detection:
term1 AND W/5 term2
W/5 AND term1 term2
(apple OR banana AND cherry) W/3 fruit
NOT W/10 dangerous
term1 OR term2 AND term3 OR term4
